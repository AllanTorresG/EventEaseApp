@page "/"

<h3>Upcoming Events</h3>
<div class="d-flex flex-wrap">

    @foreach (var evt in EventData.EventsList)
    {
        <EventCard Id="@evt.Id" Name="@evt.Name" Date="@evt.Date" Location="@evt.Location" />
    }
</div>


